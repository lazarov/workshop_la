- if flash[:error]
  %p#error= flash[:error]
%h1 Editing category


= render 'form'

%br

= link_to 'Show', category, class: 'btn btn-primary'
= link_to 'Back', categories_path, class: 'btn btn-primary'
